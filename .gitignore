# OS cruft
Thumbs.db
Desktop.ini
.DS_Store

# Editors
.vs/
.vscode/
*.swp

# Your personal overlay config (the committed template is overlay-config.sample.json)
overlay-config.json

# User-dropped icons; keep only the bundled NMS10 community logo
assets/*
!assets/nms10-logo.png

# Local record of the NMS10 logo usage approval (not for the repo)
nms10-logo-approval.png
